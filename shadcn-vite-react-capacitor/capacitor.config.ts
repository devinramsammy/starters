import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.template.app',
  appName: 'shadcn-vite-react-capacitor',
  webDir: 'dist'
};

export default config;
